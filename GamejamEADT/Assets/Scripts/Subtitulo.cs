using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Subtitulo : MonoBehaviour
{
/*
    GameObject Subtitulo;
    GameObject Textosub;
    public string Text;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    
    void OnTriggerEnter(Collider other)
    {
        if(other.gameObject.tag == "Player")
        {
            Subtitulo = GameObject.Find("Subtitulo");
            Subtitulo.SetActive(true);
            Textosub = GameObject.Find("Subtitulo");
            Textosub.GetComponent<Text>().text = Text;

        }
       
    }

    void OnTriggerExit(Collider other)
    {
        if (other.gameObject.tag == "Player")
        {
            Subtitulo = GameObject.Find("Subtitulo");
            Subtitulo.SetActive(false);
            

        }

    }*/
}
